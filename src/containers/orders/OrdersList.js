import React from 'react';
import { Route, Link } from 'react-router-dom'

const OrdersList = props => (

    <div>
        Orders List size: {props.ordersList.length}
    </div>
       
)

export default OrdersList